services:
  web:
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
      args:
        - VITE_API_URL=http://185.215.165.2:3001/api/v1
    ports:
      - "8090:80"
    restart: unless-stopped
